import chai from 'chai';
import {expect} from 'chai';
import sinon from 'sinon';
import "../src/main";

describe("doPost", function(){
  beforeEach(() => {
    // var getProperty = sinon.stub(UserProperties, "getProperty").withArgs("TWITTER_API_KEY").returns("key");
    // var stub = sinon.stub(PropertiesService, "getUserProperties").returns(getProperty);
  });

})
